services:
  backend:
    build:
      context: .
    container_name: whitenet-subscription
    restart: unless-stopped
    env_file:
      - .env

    ports:
      - "${APP_PORT}:${APP_PORT}"
    volumes:
      - ./frontend:/app/frontend
